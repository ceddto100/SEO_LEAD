# ═══════════════════════════════════════════════════════════════════════════════
# SEO_LEAD AUTOMATION PLATFORM — MASTER CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in your real values:
#   copy .env.example .env
#
# All 12 workflows read from this single .env file.
# Lines starting with # are comments. Do NOT wrap values in quotes.
# ═══════════════════════════════════════════════════════════════════════════════


# ── AI PROVIDER ──────────────────────────────────────────────────────────────
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Model to use for all AI operations (gpt-4o recommended for quality)
# Options: gpt-4o | gpt-4-turbo | gpt-3.5-turbo (cheaper but lower quality)
OPENAI_MODEL=gpt-4o

# Maximum tokens for AI responses (higher = longer responses, more cost)
OPENAI_MAX_TOKENS=4096


# ── SEO DATA PROVIDER (DataForSEO) ──────────────────────────────────────────
# Sign up at: https://app.dataforseo.com/register
# Your login email and API password (found in Account > API Access)
DATAFORSEO_LOGIN=your@email.com
DATAFORSEO_PASSWORD=your_api_password

# Target country for keyword data (numeric code)
# 2840 = United States | 2826 = United Kingdom | 2124 = Canada | 2036 = Australia
SEO_LOCATION_CODE=2840

# Target language for keyword data
SEO_LANGUAGE_CODE=en


# ── GOOGLE SHEETS ────────────────────────────────────────────────────────────
# 1. Create a service account: https://console.cloud.google.com → APIs → Credentials
# 2. Enable Google Sheets API and Google Drive API
# 3. Download the JSON key file and place it in credentials/ folder
# 4. Share your Google Sheet with the service account email (Editor access)
GOOGLE_SERVICE_ACCOUNT_JSON=credentials/service_account.json

# The ID from your Google Sheet URL:
# https://docs.google.com/spreadsheets/d/THIS_IS_THE_ID/edit
GOOGLE_SHEET_ID=your_google_sheet_id_here


# ── NOTIFICATIONS ────────────────────────────────────────────────────────────
# How to receive workflow completion alerts
# Options: email | slack | none
NOTIFICATION_METHOD=email

# --- Email (SMTP) Settings ---
# For Gmail: use smtp.gmail.com / 587, and generate an App Password:
# https://myaccount.google.com → Security → 2-Step Verification → App Passwords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@gmail.com
SMTP_PASSWORD=your_gmail_app_password
NOTIFICATION_TO=you@gmail.com

# --- Slack Settings (optional, if NOTIFICATION_METHOD=slack) ---
# Create an Incoming Webhook: https://api.slack.com/messaging/webhooks
SLACK_WEBHOOK_URL=


# ── WEBSITE / NICHE ──────────────────────────────────────────────────────────
# Your business niche (used as context for AI content generation)
NICHE=digital marketing

# Your website's base URL (used for link building, indexing, publishing)
SITE_URL=https://yourdomain.com


# ── WORDPRESS PUBLISHING (Workflow 05) ───────────────────────────────────────
# WordPress site URL (e.g. https://yourblog.com)
# Leave as-is if not using WordPress — the workflow won't crash in dry-run.
WORDPRESS_URL=https://yourdomain.com

# WordPress Application Password or JWT token for REST API auth
# Create one at: WordPress Admin > Users > Profile > Application Passwords
WORDPRESS_TOKEN=your_wordpress_app_password


# ── WORKFLOW SETTINGS ────────────────────────────────────────────────────────
# Minimum monthly search volume to keep a keyword (filters out low-value terms)
MIN_KEYWORD_VOLUME=100

# How many top-opportunity keywords to push to the Content Queue for Workflow 02
TOP_KEYWORDS_TO_QUEUE=10


# ── GENERAL ──────────────────────────────────────────────────────────────────
# Logging verbosity: DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# Dry-run mode: true = skip all API calls, use mock/sample data for testing
# Set to true when first testing the system before adding real API keys
DRY_RUN=false
